/**
 * The academy's own name, as the candidate's stand-in for the mounted template.
 *
 * Production reads it from `template.data.json`; the candidate hard-codes one value because the
 * point of these states is the ENTRY anatomy, not the template mechanism, which the landing already
 * proves. Recorded here so nobody mistakes the constant for a design decision.
 */
export const ACADEMY_NAME = "Học viện Mộc" // vn-ok: one academy's own name, rendered verbatim
