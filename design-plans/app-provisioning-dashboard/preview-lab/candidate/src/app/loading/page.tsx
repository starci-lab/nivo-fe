import { ResourcesPage } from "../../components/pages/ResourcesPage"

/** Review route isolating the `loading` state of the resource list. Not materialized. */
const Route = () => <ResourcesPage phase="loading" />

export default Route
