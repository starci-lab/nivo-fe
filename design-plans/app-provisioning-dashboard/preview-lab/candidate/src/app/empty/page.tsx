import { ResourcesPage } from "../../components/pages/ResourcesPage"

/** Review route isolating the `empty` state of the resource list. Not materialized. */
const Route = () => <ResourcesPage phase="empty" />

export default Route
