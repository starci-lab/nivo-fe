import { ResourceDetailPage } from "../../components/pages/ResourceDetailPage"

/** Review route isolating the `notFound` state of one resource. Not materialized. */
const Route = () => <ResourceDetailPage phase="notFound" />

export default Route
