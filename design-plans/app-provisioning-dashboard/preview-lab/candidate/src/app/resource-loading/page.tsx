import { ResourceDetailPage } from "../../components/pages/ResourceDetailPage"

/** Review route isolating the `loading` state of one resource. Not materialized. */
const Route = () => <ResourceDetailPage phase="loading" />

export default Route
