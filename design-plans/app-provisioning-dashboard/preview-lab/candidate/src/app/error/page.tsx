import { ResourcesPage } from "../../components/pages/ResourcesPage"

/** Review route isolating the `error` state of the resource list. Not materialized. */
const Route = () => <ResourcesPage phase="error" />

export default Route
