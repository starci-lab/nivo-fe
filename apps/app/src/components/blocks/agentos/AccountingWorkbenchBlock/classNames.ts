/** Resolved application-owned stack between Accounting regions. */
export const ACCOUNTING_WORKBENCH_CLASS_NAME = "flex min-w-0 flex-col gap-6";
/** Resolved application-owned stack inside one Accounting region. */
export const ACCOUNTING_FIELD_STACK_CLASS_NAME = "flex min-w-0 flex-col gap-4";
/** Resolved wrapping relationship between peer Accounting actions. */
export const ACCOUNTING_ACTION_ROW_CLASS_NAME = "flex min-w-0 flex-wrap gap-2";
/** Responsive split between immutable controls and the signed balance fact. */
export const ACCOUNTING_SUMMARY_GRID_CLASS_NAME = "grid min-w-0 gap-4 md:grid-cols-2 md:items-start";
/** Mobile-first form grid that preserves one-column reading order. */
export const ACCOUNTING_FORM_GRID_CLASS_NAME = "grid min-w-0 gap-4 md:grid-cols-2";
/** Two peer operational cards that collapse without horizontal scrolling. */
export const ACCOUNTING_OPERATIONS_GRID_CLASS_NAME = "grid min-w-0 gap-4 lg:grid-cols-2";
/** A field that spans the enhanced two-column form without changing mobile order. */
export const ACCOUNTING_FORM_FULL_SPAN_CLASS_NAME = "min-w-0 md:col-span-2";
/** Resolved application-owned padding and rhythm for an Accounting record. */
export const ACCOUNTING_ROW_CLASS_NAME = "flex min-w-0 flex-col gap-2 px-4 py-3";
/** Resolved label-to-native-control relationship for published Grammar gaps. */
export const ACCOUNTING_NATIVE_FIELD_CLASS_NAME = "flex min-w-0 flex-col gap-2";
/** Resolved application-owned measure and inset for semantic native controls. */
export const ACCOUNTING_NATIVE_CONTROL_CLASS_NAME = "min-h-11 w-full px-4 py-3";
