/** Uniform ledger rows: one declaration draws every line between them (BOUNDARY-3). */
export const MODULE_LEDGER_ROWS_CLASS_NAME = "divide-y divide-separator";

/** One ledger row: copy stack and trailing group as peers, inset like a flush-surface band (GAP-3, PADDING-4, PADDING-3). */
export const MODULE_LEDGER_ROW_CLASS_NAME = "flex items-center justify-between gap-3 px-4 pt-3 pb-3";

/** The name of the row over its one qualifier (GAP-1). */
export const MODULE_LEDGER_COPY_CLASS_NAME = "flex flex-1 flex-col gap-1";

/** The badges of the row beside its one action (GAP-2). */
export const MODULE_LEDGER_TRAILING_CLASS_NAME = "flex items-center gap-2";
