import { cn } from "@heroui/react";

/** Stable column rhythm for the single-business module surface. */
export const BUSINESS_MODULES_CLASS_NAME = cn("flex", "min-w-0", "flex-col", "gap-4");

/** Keep workspace identity facts together without turning them into a selector. */
export const WORKSPACE_IDENTITY_CLASS_NAME = cn("flex", "flex-wrap", "items-center", "gap-2");
